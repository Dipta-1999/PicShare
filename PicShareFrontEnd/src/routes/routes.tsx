import { createBrowserRouter } from "react-router-dom";
import Navbar3 from "../components/navbar-initial";
const router = createBrowserRouter([

    { path: "/", element: <Navbar3 /> }

]);
export default router;